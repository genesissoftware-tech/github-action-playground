name: Docker Run

on:
  push:
    branches: [ main, docker-image ]
    paths-ignore:
      - 'docs/**'
      - '**.md'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  compiler-v9:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler-version: ["8", "9"]
    container: mosnamarek/arm32-toolchain:${{ matrix.compiler-version }}
    steps:
      - name: Checkout project
        uses: actions/checkout@v2

      - name: CMake configure project
        run: cmake -S . -B build

      - name: Build project
        run: make --directory build

      - name: Run tests on project
        run: make --directory build test

      - name: Archive test results
        uses: actions/upload-artifact@v2
        with:
          name: unit-test-report-v${{ matrix.compiler-version }}
          path: build/Testing/Temporary/LastTest.log
